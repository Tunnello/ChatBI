# ChatBI 后端环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================
# 必需配置
# ============================================

# 阿里百炼 API Key（必需）
# 获取方式：访问 https://dashscope.aliyuncs.com/ 创建 API Key
OPENAI_API_KEY=your_qwen_api_key_here

# ============================================
# 可选配置
# ============================================

# API 基础 URL（可选，默认值如下）
# 如果使用其他兼容 OpenAI API 的服务，修改此 URL
OPENAI_API_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 服务器配置（可选）
HOST=0.0.0.0          # 服务器监听地址，默认 0.0.0.0
PORT=8000            # 服务器端口，默认 8000

# 环境模式（可选）
# local: 开发模式，启用热重载
# production: 生产模式
ENV=local

# 日志配置（可选）
LOG_PATH=logs/server.log  # 日志文件路径，默认 logs/server.log
