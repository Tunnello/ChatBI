name: nlp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # 使用 pip 安装一些 conda 没有的包
  - pip:
    # 从 pyproject.toml 转换的依赖
    - chromadb>=1.0.20
    - duckduckgo-search==6.3.0
    - faker>=37.6.0
    - ipykernel>=6.30.1
    - langchain==0.3.3
    - langchain-community==0.3.2
    - langchain-mcp-adapters>=0.1.9
    - langchain-openai==0.3.3
    - langgraph==0.2.38
    - pandas>=2.3.2
    - pydantic==2.9.2
    - python-magic>=0.4.27
    - requests==2.32.3
    - streamlit==1.49.0
    - streamlit-highcharts==0.1.5
    - tiktoken>=0.11.0
    - tqdm>=4.67.1
    # 额外可能需要的依赖
    - python-dotenv  # 用于加载 .env 文件

